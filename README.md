Fork this repo, set to private. Clone and then run `install.sh`. Then source `~/.bashrc` or `~/.bash_profile`.

`notebook` will then be executable from anywhere.

To open up "General" notebook: `notebook General`. 
